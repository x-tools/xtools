* [[{{ page.title }}]]
* {{ project.domain }}
* {{ msg('total-edits') }}: {{ ai.numRevisions|number_format }}
* {{ msg('editors') }}: {{ ai.numEditors|number_format }}
* {{ 'now'|date('Y-m-d H:i:s') }}

<sup>1</sup> {{ msg('edits') }}<br />
<sup>2</sup> {{ msg('average-time-bw-edits') }}
{| class="wikitable sortable"
! {{ msg('username') }}
!#<sup>1</sup>
! {{ msg('minor-edits') }}
!%
!{{ msg('first-edit') }}
!{{ msg('latest-edit') }}
!{{ msg('average-time-bw-edits-abbr') }}<sup>2</sup>
!{{ msg('added-bytes') }}
|-
{% for editor, stats in ai.editors|slice(0, editorlimit ) %}
| [[ {{ attribute(project.namespaces, 2) }}:{{ editor }}|{{ editor }} ]] {#
#} || {{ stats.all|number_format }} {#
#} || {{ stats.minor|number_format }} {#
#} || {{ stats.minor|percent_format(stats.all) }} {#
#} || {{ stats.first|date('Y-m-d H:i') }} {#
#} || {{ stats.last|date('Y-m-d H:i') }} {#
#} || {{ stats.atbe|number_format(1) }} {#
#} || {{ stats.added|number_format }}
|-
{% endfor %}
|}
