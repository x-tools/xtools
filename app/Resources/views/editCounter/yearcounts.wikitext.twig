{% import 'macros/wiki.html.twig' as wiki %}
{% if not is_sub_request %}
== [[User:{{ user.username }}]] ==

{% set link %}[{{ url('EditCounterYearCounts', {'username': user.username, 'project': project.domain}) }} {{ msg('xtools-title') }}]{% endset %}
{{ msg('xtools-advert', [link, date()|date_format]) }}
{% endif %}

=== {{ msg('year-counts') }} ===

{| class="wikitable sortable"
! {{ msg('year') }}
! {{ msg('count') }}
|-
{% set labels = ec.yearCounts.monthLabels %}
{% for year,total in ec.yearTotals %}
| {{ labels[loop.index0] }}
| {% verbatim %}{{FORMATNUM:{% endverbatim %}{{ total }}}}
|-
{% endfor %}
|}
