{% extends 'base.html.twig' %}
{% import 'macros/forms.html.twig' as forms %}

{% block body %}
    <div class="col-lg-12">
        <a href="{{ path("quoteRandom") }}">{{ msg("quote_random") }}</a>
        <br />
        <ul>
            {% for quote in quotes %}
                <li><a href="{{ path("quoteID", {"id": loop.index0}) }}">#{{ loop.index0 }}</a>: {{ quote }}</li>
            {% endfor %}
        </ul>
    </pre>

    </div>
{% endblock %}
