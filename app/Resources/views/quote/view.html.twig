{% extends 'base.html.twig' %}
{% import 'macros/forms.html.twig' as forms %}

{% block body %}
    <div class="col-lg-12">
        {{ msg("quote_id", id) }} &middot; <a href="{{ path("quoteRandom") }}">{{ msg("quote_random") }}</a> &middot; <a href="{{ path("quoteAll") }}">{{ msg("quote_showall") }}</a>
        <pre>{{ text }}</pre>

    </div>
{% endblock %}
